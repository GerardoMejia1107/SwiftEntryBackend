info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: "{{swift_entry_base_uri}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "holly@gmail.com",
        "password": "holly_and_michael"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
