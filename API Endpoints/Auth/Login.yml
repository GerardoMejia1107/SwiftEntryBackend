info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{swift_entry_base_uri}}/login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
