info:
  name: Validate ticket
  type: http
  seq: 3

http:
  method: POST
  url: "{{swift_entry_base_uri}}/tickets/validate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
