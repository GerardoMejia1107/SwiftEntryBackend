info:
  name: Get my tickets
  type: http
  seq: 2

http:
  method: GET
  url: "{{swift_entry_base_uri}}/tickets/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
