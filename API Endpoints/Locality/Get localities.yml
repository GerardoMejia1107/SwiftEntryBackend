info:
  name: Get localities
  type: http
  seq: 2

http:
  method: GET
  url: "{{swift_entry_base_uri}}/localities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
