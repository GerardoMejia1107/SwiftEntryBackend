info:
  name: Delete locality by id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{swift_entry_base_uri}}/localities/30"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
