info:
  name: Get events by organizer id
  type: http
  seq: 5

http:
  method: GET
  url: "{{swift_entry_base_uri}}//events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
