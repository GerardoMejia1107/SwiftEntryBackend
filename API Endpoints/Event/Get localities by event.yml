info:
  name: Get localities by event
  type: http
  seq: 6

http:
  method: GET
  url: "{{swift_entry_base_uri}}/localities/event/14"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
