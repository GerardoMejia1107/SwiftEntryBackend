info:
  name: Update event
  type: http
  seq: 4

http:
  method: PUT
  url: "{{swift_entry_base_uri}}/events/20"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
