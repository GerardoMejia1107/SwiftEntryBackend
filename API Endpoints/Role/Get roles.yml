info:
  name: Get roles
  type: http
  seq: 2

http:
  method: GET
  url: "{{swift_entry_base_uri}}/roles"
  auth:
    type: basic
    username: mike
    password: holly

runtime:
  variables:
    - name: swift_entry_base_uri
      value: http://localhost:8080/swift_entry

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
