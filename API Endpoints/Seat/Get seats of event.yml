info:
  name: Get seats of event
  type: http
  seq: 6

http:
  method: GET
  url: "{{swift_entry_base_uri}}/seats/event/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
