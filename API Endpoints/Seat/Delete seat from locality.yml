info:
  name: Delete seat from locality
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{swift_entry_base_uri}}/seats/assignment/{localitySeatId}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
