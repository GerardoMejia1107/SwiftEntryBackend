info:
  name: Get seats
  type: http
  seq: 2

http:
  method: GET
  url: "{{swift_entry_base_uri}}/seats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
