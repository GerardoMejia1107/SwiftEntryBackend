info:
  name: Get seats
  type: http
  seq: 2

http:
  method: GET
  url: "{{swift_entry_base_uri}}/seats/67"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
