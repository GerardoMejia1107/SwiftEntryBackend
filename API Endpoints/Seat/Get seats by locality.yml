info:
  name: Get seats by locality
  type: http
  seq: 3

http:
  method: GET
  url: "{{swift_entry_base_uri}}/seats/locality/26"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
