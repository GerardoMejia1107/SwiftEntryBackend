jwt:
  secret-key: "JinxCyberPunkEffectPropagationsB"
  expiration-ms: 86400000

spring:
  application:
    name: SwiftEntryBackend
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    url: ${DB_URI}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    user:
      password: holly
      name: mike
